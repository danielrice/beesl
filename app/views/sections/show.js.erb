/* Prototype + jQuery */
jQuery(document).ready(function($){
	
	var updateStr = "<%= escape_javascript(render :partial => 'show', :locals => { :page => @page }) %>";
	// $("#show_page").update(updateStr);
	
	
}